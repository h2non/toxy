{
  "name": "toxy",
  "version": "0.1.0-alpha.0",
  "description": "Hackable HTTP proxy to simulate multiple server failures and unexpected conditions",
  "repository": "h2non/toxy",
  "author": "Tomas Aparicio",
  "license": "MIT",
  "directories": {
    "lib": "./lib"
  },
  "keywords": [
    "http",
    "proxy",
    "toxic",
    "fault",
    "tolerant",
    "testing",
    "resilency",
    "fuzz",
    "evil",
    "testing",
    "failure",
    "resilient",
    "retry",
    "unexpected",
    "backoff"
  ],
  "engines": {
    "node": ">= 0.12"
  },
  "scripts": {
    "test": "./node_modules/.bin/mocha --timeout 5000 --reporter spec --ui tdd test/*",
    "nar": "nar create -e --os linux --arch x64 && nar create -e --os darwin --arch x64"
  },
  "dependencies": {
    "midware": "^0.1.3",
    "rocky": "^0.3.3"
  },
  "devDependencies": {
    "chai": "^3.0.0",
    "mocha": "^2.2.5",
    "sinon": "^1.15.3",
    "supertest": "^1.0.1"
  }
}
